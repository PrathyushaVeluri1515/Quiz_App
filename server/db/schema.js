/* Tables 
user
Mentor 
Review 
recommend_student table
*/
export const table = {
    users : "users"
}

export const userSchema = `id varchar(36) primary key,username varchar(255),password varchar(255), email varchar(255)`;